global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: log-monitor-inference
    metrics_path: /metrics
    static_configs:
      - targets:
          - inference:8000
        labels:
          service: log-monitor-inference
