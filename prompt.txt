You are a highly accurate log classification system. 
I have attached a csv file containing log messages. 

For EACH log message in the array, perform ONE task:
1. CLASSIFICATION: Classify the log into exactly one of the following four categories. 

*** CATEGORIES ***
- "CONFIGURATION": Logs related to application settings, environment variables, missing hostnames/ports, credentials, or startup configurations.
- "SYSTEM": Logs related to the underlying infrastructure (e.g., CPU, memory, disk space, kernel, or physical network limits).
- "Error": A developer made a code-level impact that must be addressed (e.g., unhandled exceptions, syntax errors, null pointers).
- "Noise": General logs, process cancellations, standard info, or anything that does not fit the above.

*** CRITICAL RULES OF PRECEDENCE ***
1. DO NOT rely on the literal word "Error" or "Exception". A log might start with "Error:" but actually be a "CONFIGURATION" issue (e.g., a missing MAIL_HOST or database credential). 
2. ALWAYS classify based on the ROOT CAUSE of the failure, not the superficial keywords.
3. If a log is caused by a missing environment variable, empty host, or bad configuration file, it MUST be classified as "CONFIGURATION", even if it caused the app to crash.

Return ONLY a labeled valid csv file with two columns "LogMessage" and "class", containing an array of objects in the EXACT SAME ORDER as the input array.
Example output format: 
{
  "results": [
    {"class": "Error"},
    {"class": "SYSTEM"},
    {"class": "CONFIGURATION"},
    {"class": "Noise"}
  ]
}